let total_tip = 0               // total tip = bill * tip percentage
let total_bill = 0              // total bill = bill + tip
let share_per_person = 0        // share per person = total bill / people

let billInputs = {
    people: document.getElementById('people'), 
    bill: document.getElementById('bill'), 
    custom: document.getElementById('custom'),
}

function totalTip(billInputs)
{
    // a function that calculates the total tip

    return total_tip
}

function totalBill(billInputs)
{
    //  a funtion that calculates the total bill

    return total_bill
}

function shareOfPeople(billInputs)
{
    // a function that calculates the share of every person

    return share_per_person
}

// a function that would display outputs

function display()
{
    // display calculations
}